// ============================================================
//  WiFi 접속 정보 — 이 파일을 wifi_secrets.h로 복사해 실제 값 기입
//  (wifi_secrets.h는 .gitignore 처리 — 저장소에 올라가지 않음)
//
//  폰 핫스팟 SSID/비밀번호를 적으면 부팅 시 핫스팟에 붙고(STA),
//  비워 두거나 접속 실패하면 자체 AP(config.h의 AP_SSID)로 동작한다.
// ============================================================
#pragma once

#define WIFI_SSID "YOUR_PHONE_HOTSPOT"
#define WIFI_PASS "YOUR_HOTSPOT_PASSWORD"
